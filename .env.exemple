DB_HOST=localhost
DB_PORT=5432
DB_NAME=maxitsa
DB_USER=utilisateur
DB_PASSWORD=motdepasse
BASE_URL=http://localhost:8080/
TWILIO_SID= # Optionnel, pour l'envoi de SMS
TOKEN=      # Optionnel, pour l'envoi de SMS
PHONE=      # Optionnel, pour l'envoi de SMS
MESSAGING_SID= # Optionnel, pour l'envoi de SMS
DSN=        # Optionnel, généré automatiquement